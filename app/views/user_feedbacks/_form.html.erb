<%= form_with scope: :user_feedbacks, url: user_feedbacks_path,
             html: { class: "flex h-full flex-col form #{action_name}" } do |f| %>
  <div class="absolute right-0 top-0 hidden pr-4 pt-4 sm:block">
    <button type="button"
            class="rounded-md bg-white text-gray-400 hover:text-gray-500 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2"
            data-action="click->modal-form#close:stop:prevent">
      <span class="sr-only">Close</span>
      <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
        <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
      </svg>
    </button>
  </div>

  <div class="flex items-center">
    <div class="mx-auto flex h-12 w-12 flex-shrink-0 items-center justify-center rounded-full bg-primary-100 sm:mx-0 sm:h-10 sm:w-10">
      <%= inline_svg_tag "@tabler/icons/icons/outline/message-chatbot.svg", class: 'h-6 w-6 text-primary-600' %>
    </div>

    <div class="mt-3 text-center sm:ml-4 sm:mt-0 sm:text-left">
      <h3 class="text-base font-semibold leading-6 text-gray-900">Lasciaci un Feedback</h3>
    </div>
  </div>

  <div class="mt-2">
    <p class="text-sm text-gray-500">
      Ogni idea è una buona idea, soprattutto da parte tua. La tua opinione è importante per noi.
      Lasciaci un feedback per migliorare la tua esperienza.
    </p>
  </div>

  <!-- Form Content -->
  <div class="mt-2 grid grid-cols-1 gap-y-6 gap-x-4">
    <%= form.text_area :message, placeholder: 'Mi piacerebbe tanto che ...' %>
  </div>

  <!-- Form Actions -->
  <div class="form-actions pt-5">
    <%= form.submit "Invia Feedback", class: 'inline-flex w-full', 'data-turbo-submits-with': 'Invio in corso' %>
  </div>
<% end %>
